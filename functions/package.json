{
  "name": "guardian-portal-functions",
  "description": "Cloud Functions — PIX, assinatura, pairing e e-mail de boas-vindas",
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "scripts": {
    "lint": "node --check src/index.js && node --check src/mercadopago.js && node --check src/subscription.js && node --check src/device_pairing.js && node --check src/welcome_email.js && node --check src/welcome_email_template.js",
    "test": "node --test src/welcome_email_template.test.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  },
  "private": true
}
