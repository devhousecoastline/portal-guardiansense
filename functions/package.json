{
  "name": "guardian-portal-functions",
  "description": "Cloud Functions — PIX, assinatura e pairing de aparelho",
  "engines": {
    "node": "22"
  },
  "main": "src/index.js",
  "scripts": {
    "lint": "node --check src/index.js && node --check src/mercadopago.js && node --check src/subscription.js && node --check src/device_pairing.js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  },
  "private": true
}
